import Axios from "axios";

//const endpoint = `${process.env.LOCATION_API_URL}Region/`
const endpoint = `${process.env.REACT_APP_LOCATION_SERVER_API_URL}Province/`;

export async function getAllProvinces() {
  ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("ENDPOINT", endpoint)
  try {
    let allProvinces = await Axios.get(`${endpoint}`);
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("USER REGIONS", allRegions)
    /*allUserRegions = allUserRegions.data.map(item => {
            return {
                _id: item._id,
                key: item.key,
                label: item.name
            }
        })*/
    return allProvinces;
  } catch (error) {
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("ERROR", error)
  }
}

export async function getFilteredProvinces(id: string) {
  //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("ENDPOINT", endpoint)
  try {
    let allProvinces = await Axios.get(`${endpoint}${id}`);
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("USER REGIONS", allRegions)
    allProvinces = allProvinces.data.map((item: any) => {
      return {
        _id: item._id,
        label: item.name,
      };
    });

    // //////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("ALL PROVINCES", allProvinces)
    return allProvinces;
  } catch (error) {
    ////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////////console.log  ("ERROR", error)
  }
}
